{
  "name": "{{PROJECT_NAME}}",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "packet dev",
    "build": "packet build",
    "start": "node dist/index.js",
    "deploy": "packet deploy"
  },
  "dependencies": {
    "@packet/core": "^1.0.0-beta.1",
    "@packet/auth": "^1.0.0-beta.1",
    "@packet/db": "^1.0.0-beta.1",
    "@packet/router": "^1.0.0-beta.1"
  },
  "devDependencies": {
    "@packet/testing": "^1.0.0-beta.1",
    "typescript": "^5.3.3",
    "tsx": "^4.7.0"
  }
}
